'use strict';

describe('w2dApp.places module', function() {

  beforeEach(module('w2dApp.places'));

  describe('places controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var placesCtrl = $controller('PlacesCtrl');
      expect(placesCtrl).toBeDefined();
    }));

  });
});