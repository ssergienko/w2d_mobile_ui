'use strict';

describe('w2dApp.map module', function() {

  beforeEach(module('w2dApp.map'));

  describe('map controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mapCtrl = $controller('MapCtrl');
      expect(mapCtrl).toBeDefined();
    }));

  });
});